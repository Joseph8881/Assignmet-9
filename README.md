# Assignmet #9

# Django MongoDB Lease App

This project is a Django web application that connects to a MongoDB database hosted on an AWS EC2 instance.

## Features
- Add lease records (MAC, IP, DHCP)
- View stored records
- MongoDB integration using PyMongo

## Run
python manage.py runserver